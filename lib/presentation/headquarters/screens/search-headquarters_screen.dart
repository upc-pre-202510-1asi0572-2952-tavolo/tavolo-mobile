import 'package:flutter/material.dart';

// Esta pantalla permite buscar sedes disponibles en la aplicación Tavolo Mobile.
class SearchHeadquartersScreen extends StatelessWidget {
  const SearchHeadquartersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lista de Sedes'),
      ),

    );
  }

}